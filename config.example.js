// Copy this file to config.js and set your real CSV URLs there.
// Keep config.js out of version control for security.
window.APP_CONFIG = {
  // Set BEER_CSV_URL to your published Google Sheet CSV export URL
  BEER_CSV_URL: "https://docs.google.com/spreadsheets/d/.../pub?gid=0&single=true&output=csv",

  // Optional: FOOD_CSV_URL
  FOOD_CSV_URL: ""
};
