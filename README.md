# Mp Product Education

Instructions:

1. Copy config.example.js to config.js and set BEER_CSV_URL to the published Google Sheet CSV URL.
2. Run locally: `npm start` (uses npx http-server so no global install needed).

Do NOT commit config.js — it is ignored by .gitignore to keep your data out of the repo.
